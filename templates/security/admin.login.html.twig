<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">

    <title>Pièce de monnaie en or commémorative de 50 Ariary - Authentification</title>
    <meta content="" name="description">
    <meta content="" name="keywords">

    <!-- Favicons -->
    <link href="{{ asset('img/BFM50LOGOFINAL-01.png') }}" rel="icon">
    <link href="{{ asset('img/BFM50LOGOFINAL-01.png') }}" rel="apple-touch-icon">

    <!-- Google Fonts -->
    <link href="https://fonts.gstatic.com" rel="preconnect">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Nunito:300,300i,400,400i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">

    <!-- Vendor CSS Files -->
    <link href="{{ asset('vendor/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('vendor/bootstrap-icons/bootstrap-icons.css') }}" rel="stylesheet">

    <!-- Template Main CSS File -->
    <link href="{{ asset('css/front.css') }}" rel="stylesheet">

</head>

<body>

<div id="spinner" class="spinner bg-white d-none">
    <div class="m-auto">
        <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Chargement...</span>
        </div>
    </div>
</div>

<main id="main" class="main-index">

{#    <div id="test" class="row">#}
{#        <div class="col-12">#}
{#            <h1 class="text-center m-auto p-2">VERSION TEST</h1>#}
{#        </div>#}
{#    </div>#}

    <div class="row">
        <div class="col-lg-6">

            <div class="row bg-light-blue mb-5{# loginHeaderTest#}">
                <div class="col-12 ">
                    <div class="header-login">
                        <div class="login-wrapper-logo">
                            <a href="{{ path('app_index') }}"><img src="{{ asset('img/BFM50LOGOFINAL-01.png') }}" alt="BFM" class="login-logo"></a>
                        </div>

                        <div class="wrapper-title m-auto">
                                <div class="row my-auto">
                                    <div class="col-12 text-gold  text-uppercase">
                                        <h3 class="title_login">pièces de monnaie en or commémorative</h3>
                                    </div>
                                </div>
                                <div class="row my-auto">
                                    <div class="col-12 text-gold  text-uppercase">
                                        <h3 class="title_login">banky foiben'i madagasikara</h3>
                                    </div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    <div class="row mb-4">
                        <div class="offset-2 col-8">
                            <h2 class="text-gold">Authentification au Back Office</h2>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">

                            <form action="{{ path('admin_login') }}" method="post">

                                <div class="row">
                                    <div class="offset-2 col-8">
                                        <label for="username" class="form-label">Identifiant</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi-person-fill"></i></span>
                                            <input type="text" id="username" name="_username" class="form-control" value="{{ last_username }}">
                                        </div>

                                    </div>
                                    <div class="offset-2 col-8 mt-4 mb-2">
                                        <label for="password" class="form-label">Mot de passe</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi-lock-fill"></i></span>
                                            <input type="password" id="password" name="_password" class="form-control">
                                            <span class="input-group-text input-group-text-end show-pass"><i class="bi-eye-fill"></i></span>
                                            <span class="input-group-text input-group-text-end hide-pass d-none"><i class="bi-eye-slash-fill"></i></span>
                                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                        </div>
                                    </div>
{#                                   <div class="offset-2 col-8 mt-4 mb-2 d-flex justify-content-end"><a class="text-gold" href="{{ path('user_mdpForgot') }}" >Mot de passe oublié ?</a></div>#}

                                    <div style="text-align:center" class="mt-4">
                                        <button class="btn btn-primary pl-5 pr-5 px-5">Connecter</button>
                                    </div>

                                </div>

                            </form>


                        </div>
                    </div>
                </div>
            </div>


        </div>

        <div class="col-lg-6 p-0 right-login">
            <img src="{{ asset('img/IMG_7301.jpeg') }}" alt="BFM">
        </div>
    </div>

</main><!-- End main -->

<!-- Vendor JS Files -->
<script src="{{ asset('vendor/jquery/dist/jquery.min.js') }}"></script>
<script src="{{ asset('vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
<script src="{{ asset('node_modules/sweetalert2/dist/sweetalert2.all.min.js') }}"></script>
<script src="{{ asset('js/alert.js') }}"></script>

<script>
    $(document).ready(function () {
        {% if error %}
            modalAlert('{{ error.message }}', 'error');
        {% endif %}

        {% for message in app.flashes('success') %}
            modalAlert('{{ message }}', 'success');
        {% endfor %}

        {% for message in app.flashes('error') %}
            modalAlert('{{ message }}', 'error');
        {% endfor %}

        $('.show-pass').off('click').on('click', function () {
            $(this).addClass('d-none');
            $('.hide-pass').removeClass('d-none');
            $("#password").attr('type', 'text');
        });

        $('.hide-pass').off('click').on('click', function () {
            $(this).addClass('d-none');
            $('.show-pass').removeClass('d-none');
            $("#password").attr('type', 'password');
        });
    });
</script>

</body>

</html>
