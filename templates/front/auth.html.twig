<!DOCTYPE html>
<html lang="en" class="h-100">

    <head>
        <meta charset="utf-8">
        <meta content="width=device-width, initial-scale=1.0" name="viewport">

        <title>Pièce de monnaie en or commémorative de 50 Ariary - Authentification</title>
        <meta content="" name="description">
        <meta content="" name="keywords">

        <!-- Favicons -->
        <link href="{{ asset('img/BFM50LOGOFINAL-01.png') }}" rel="icon">
        <link href="{{ asset('img/BFM50LOGOFINAL-01.png') }}" rel="apple-touch-icon">

        <!-- Google Fonts -->
        <link href="https://fonts.gstatic.com" rel="preconnect">
        <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Nunito:300,300i,400,400i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">

        <!-- Vendor CSS Files -->
        <link href="{{ asset('vendor/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
        <link href="{{ asset('vendor/bootstrap-icons/bootstrap-icons.css') }}" rel="stylesheet">

        <!-- Template Main CSS File -->
        <link href="{{ asset('css/front.css') }}" rel="stylesheet">

    </head>

    <body  class="d-flex flex-column h-100">

        <div id="spinner" class="spinner bg-white d-none">
            <div class="m-auto">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Chargement...</span>
                </div>
            </div>
        </div>

        <main id="main" class="main-index">

            <div class="row">
                <div class="col-lg-6">

                    <div class="row bg-light-blue mb-5">
                        <div>
                            <div class="pt-md-0 d-flex flex-wrap header-login">
                                <div class="login-wrapper-logo">
                                    <a href="{{ path('app_index') }}"><img src="{{ asset('img/BFM50LOGOFINAL-01.png') }}" alt="BFM" class="login-logo"></a>
                                </div> 

                                <div class="wrapper-title m-auto">
                                    <div class="row my-auto">
                                        <div class="col-12 text-light-gold text-uppercase">
                                            <h3 class="title-login" >pièce de monnaie en or commémorative</h3>
                                        </div>
                                    </div>
                                    <div class="row my-auto">
                                        <div class="col-12 text-light-gold text-uppercase">
                                            <h3 class="title-login">banky foiben'i madagasikara</h3>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                    </div>

                    {{ include('front/_login.form.html.twig') }}

                </div>

                <div class="col-lg-6 p-0 right-login">
                    <img src="{{ asset('img/IMG_7301.jpeg') }}" alt="BFM">
                </div>
            </div>

        </main><!-- End main -->

        <!-- Vendor JS Files -->
        <script src="{{ asset('vendor/jquery/dist/jquery.min.js') }}"></script>
        <script src="{{ asset('vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
        <script src="{{ asset('node_modules/sweetalert2/dist/sweetalert2.all.min.js') }}"></script>
        <script src="{{ asset('js/alert.js') }}"></script>

        <script>
            $(document).ready(function () {
                {% for message in app.flashes('success') %}
                    flashAlert('success', '{{ message }}');
                {% endfor %}

                {% for message in app.flashes('error') %}
                    flashAlert('error', '{{ message }}');
                {% endfor %}
            });
        </script>

    </body>

</html>
